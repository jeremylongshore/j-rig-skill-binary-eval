{
  "name": "jrig-emit-evidence-ci",
  "version": "0.1.0",
  "private": true,
  "description": "CI-only signed-evidence emitter for the intent-eval-dashboard reports hub (repo row key: jrig). Not a workspace package, never published; installed standalone in .github/workflows/nightly-skill-evals.yml via `pnpm install --ignore-workspace`.",
  "type": "module",
  "dependencies": {
    "@intentsolutions/core": "0.9.0"
  }
}
