<!-- TOMBSTONE [9k5h.15]: this file previously vendored a verbatim extraction of
     https://agentskills.io/specification (captured 2026-03-24). That copy is
     retired — the kernel is the single source of truth for upstream spec content. -->

# AgentSkills.io Specification — moved to the kernel SSoT

This vendored copy (2026-03-24 capture) is retired. Consult instead:

1. **Kernel upstream-base projection** — `@intentsolutions/core` `schemas/authoring/v1/upstream-base/skill-frontmatter.v1.json` (machine-readable; the 2026-05-28 snapshot; what j-rig's schemas now compose from).
2. **Lab vendored single source** — `intent-eval-lab` `_vendor/upstream/skill-frontmatter/agentskills-spec-v1.0.md` (prose snapshot, fetched 2026-05-28, drift-watched).
3. **Live upstream** — <https://agentskills.io/specification>.

The synthesis/guide docs in this directory (`source-of-truth.md`, `frontmatter-spec.md`, `validation-rules.md`) remain — they are IS-authored interpretation, not upstream duplication.
