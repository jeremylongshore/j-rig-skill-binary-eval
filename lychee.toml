verbose = "info"
no_progress = true
include_verbatim = true

exclude = [
  "^https://localhost",
  "^http://localhost",
  "^https://127\\.0\\.0\\.1",
  "^https://twitter\\.com",
  "^https://x\\.com",
  "^https://linkedin\\.com",
  # SKILL.md template placeholders: {{REF_n}} link targets are Handlebars
  # tokens an author fills in, not real files. URL-encoded as %7B%7B...%7D%7D.
  "%7B%7B",
]

exclude_path = [
  ".audit-harness",
  "node_modules",
  "packages/*/dist",
]
