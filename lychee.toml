verbose = "info"
no_progress = true
include_verbatim = true

exclude = [
  "^https://localhost",
  "^http://localhost",
  "^https://127\\.0\\.0\\.1",
  "^https://twitter\\.com",
  "^https://x\\.com",
  "^https://linkedin\\.com",
]

exclude_path = [
  ".audit-harness",
  "node_modules",
  "packages/*/dist",
]

[cache]
enabled = true
max_cache_age = "7d"
